<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns the theme; this base theme just hands off to a Material3 light
         shell so the system UI (status bar, splash) renders correctly before Compose mounts. -->
    <style name="Theme.Fortress" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>
