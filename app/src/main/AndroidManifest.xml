<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Jarvis assistant: exact-time reminders, re-arm after reboot, read the
         on-device calendar for agenda + briefings. RECORD_AUDIO is intentionally
         NOT required — voice input uses the system speech-recognizer dialog. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <application
        android:name=".FortressApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fortress"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Fortress">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Firebase Cloud Messaging service: dispatches push notifications
             from the backend (e.g. when a position hits the 50% profit target). -->
        <service
            android:name=".notification.FortressFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Lock-screen "CLOSE POSITION" action target. -->
        <receiver
            android:name=".notification.CloseTradeReceiver"
            android:exported="false" />

        <!-- Jarvis: reminder alarms fire here. -->
        <receiver
            android:name=".jarvis.ReminderReceiver"
            android:exported="false" />

        <!-- Jarvis: the once-a-day briefing alarm fires here. -->
        <receiver
            android:name=".jarvis.DailyBriefingReceiver"
            android:exported="false" />

        <!-- Jarvis: re-arm reminders + briefing after a reboot. -->
        <receiver
            android:name=".jarvis.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
